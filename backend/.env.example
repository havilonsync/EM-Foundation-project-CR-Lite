ANTHROPIC_API_KEY=
DATABASE_URL=

# Comma-separated frontend origins allowed by CORS
# Local: http://localhost:3000
# Production: https://em-foundation-project-cr-lite.vercel.app
CORS_ORIGINS=http://localhost:3000

# Deployed Vercel frontend URL (also added to CORS allowlist)
FRONTEND_URL=https://em-foundation-project-cr-lite.vercel.app

# Phase 2 — Stripe donation gating + JWT sessions
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
JWT_SECRET=
SESSION_QUERY_LIMIT=10
SESSION_DURATION_HOURS=24
DONATION_AMOUNT_CENTS=500
